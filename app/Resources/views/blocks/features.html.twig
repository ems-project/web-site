<div class="col-lg-12">
	<h2 class="page-header">{{ attribute(block, 'title_'~app.request.locale )  }}</h2>
</div>

{% if attribute(block, 'body_'~app.request.locale ) %}
	<div class="col-lg-12">
		{{ attribute(block, 'body_'~app.request.locale )|internal_links|raw  }}
	</div>
{% endif %}
		


{{ render(controller(
        'AppBundle:Default:listFeatures', {marketing: true}
)) }}
